# テーブルチェックAPI連携：データ定義・裏どり結果（2026-07-15）

> 目的：TableCheck API から KDS にメニュー予約を表示するために「何のデータを・どのAPIで・どう受けるか」を1枚に集約する。
> 位置づけ：[2026-07-15 取得したい情報洗い出し](2026-07-15_テーブルチェックAPI連携_取得したい情報洗い出し.md) の続編。TableCheck 公式ドキュメント（Confluence）での裏どり結果と、KDS 実装（`kds-a-grid.html`）の実コード精査を反映した改訂版。
> 対応 Issue：#74（料金・データ形式・通知メール可否）
> **2026-07-16 追記**：本文書 §5 の残課題1〜3のうち1・2を、Booking v1 の公式APIコンソール（Swagger UI）実機確認で解決。詳細は末尾の「6. 【2026-07-16 追記】APIコンソール実機確認で確定した事項」を参照。
> **2026-07-16 訂正（保存方針）**：本文書は当初「中継サーバーがDB（reservations / reservation_items）に予約を貯める」前提で書いたが、打合せで **「注文・予約は何も保存しない（当日メモリのみ・使い捨て）」** に確定した。本文中の「当店側の格納先」列・DB格納先の記述は、将来「注文履歴を保存して分析する」構想が採用された場合の論理設計（→ `docs/db-design.md` §2 以降）への参考として読むこと。現行の中継サーバーはメモリ保持のみ（`relay-server/`）。

---

## 0. 前提：ローカルKDSとクラウドのTableCheckはどう繋がるか（1分で理解する用）

```
【クラウド】                【店内（ローカル）】
TableCheck ◀──30秒ごとに──  中継サーバー（ミニPC） ──LAN/Wi-Fi──▶ KDS（タブレット/パネル）
（予約DB）    取りに行く      当日予約をメモリ保持         window.KDS_ORDERS /
              (pull)         当日分のみ・保存しない        kds_stock_v1 を1秒ポーリング
```

- **KDSタブレットが直接TableCheckを叩くことはない**。KDSはオフライン前提の単一HTMLで、店内の中継サーバー（ミニPC）だけを見る。
- **TableCheck側から店内に通知を送り込むこと（Webhook = push）はできない**。店内サーバーはNAT/ファイアウォールの内側にあり、外から到達できないため（2026-06-04 検討済み・裏どりでも確定）。
- よって通信は**店内 → TableCheck の外向き（pull / ポーリング）一択**。TableCheck 側にはこの方式が正式に用意されている（§3）。
- 中継サーバーの役割：①TableCheckから予約を取ってくる ②**当日分のみメモリで保持する（保存しない）** ③KDSが読める形に変換して配る。（当初②を「DBに貯める」としていたが、打合せで「何も保存しない＝使い捨て」に確定。上部「2026-07-16 訂正」参照。DB化する場合の論理設計は `docs/db-design.md` §2 の将来構想に保留）

---

## 1. 使うAPIコンポーネント（⚠️ここを間違えない）

| コンポーネント | 用途 | 備考 |
| --- | --- | --- |
| **Booking v1** (`/api/booking/v1/`) | 予約データの取得。`GET /reservations?start_at_min=...&start_at_max=...`（ページネーション付き） | 1stパーティ（店舗運営者自身とその開発パートナー）のみアクセス可 = 当店は該当 |
| **Sync v1** (`/api/sync/v1/`) | 予約の作成・変更・削除の検知。`GET /sync_events?deliver=true` をポーリング | Webhookも存在するがローカル構成では使えない（§0） |
| （参考）Messaging v1 | 客へのテキストメッセージ配信 | Issue #74 の「予約通知メール可否」はこのAPIの契約可否として確認する |

- ❌ **Site Controller v1（`/api/site/v1/`）は対象外**。予約サイトアグリゲーター向けの店舗設定・卓情報の読み取り専用APIで、予約・メニュー取得の用途ではない（一度リンクを共有したが用途違いと判明）。
- APIコンソール（OpenAPI仕様）: https://api.tablecheck.com/api/booking/v1/docs / https://api.tablecheck.com/api/sync/v1/docs

---

## 2. 取得したいデータ項目と裏どり結果

### 2-1. Must（これが無いと連携する意味が薄い）

| # | 項目 | TableCheck側 | 裏どり結果 | 当店側の格納先 | KDS側の受け皿 |
| --- | --- | --- | --- | --- | --- |
| 1 | 予約ID | SyncEvent の `syncable_id` / 予約オブジェクトの `id` | ✅ 取得可能（確定） | `reservations.external_id`（冪等キー） | `stock[].rid` |
| 2 | 来店予定日時 | `start_at`（ISO8601・タイムゾーン付き。例 `2020-02-12T19:30:00+0900`） | ✅ 取得可能（確定）。JSTへの変換処理が必要 | `reservations.expected_at` | `stock[].time` |
| 3 | 人数 | `pax`（合計） | ✅ **確定（2026-07-16）**。`pax_adult` / `pax_child` / `pax_senior` / `pax_baby` が合計 `pax` とは別の独立フィールドとして存在 | `reservations.adults` / `kids` | `stock[].adults` / `kids` |
| 4 | 予約者名 | `first_name` / `last_name`（+ ふりがな用途と思われる `alt_first_name` / `alt_last_name`） | ✅ **確定（2026-07-16）** | `reservations.customer_name` | `stock[].name` |
| 5 | 予約ステータス | `status`（enum） | ✅ **確定（2026-07-16）**。enum実値: `tentative, pending, request, accepted, confirmed, attended, cancelled, noshow, rejected, iou_prepay, iou_auth` | `reservations.status` | ストックからの除去で反映 |
| 6 | **事前メニュー明細**（品名・数量） | `orders`（`ReservationOrder[]`）: `id` / `menu_item_name_translations`（多言語名。例 `{"en":"Sushi","ja":"寿司"}`）/ `price` / `qty` | ✅ **確定・最大の朗報（2026-07-16）**。memo自由記述ではなく**構造化フィールドとして取得可能**。「メニュー予約」区分が実現できる | `reservation_items.item_name` / `qty` | `stock[].menu[].name` / `qty` |

**#6 の分岐（2026-07-16 解決済み）**：
- ~~構造化データで取れる → そのまま `reservation_items` にマッピング（理想）~~ **→ これが確定。`orders[].menu_item_name_translations` / `qty` をそのままマッピングすればよい**
- ~~memo の自由テキストのみ → …~~（不要になった）
- ~~全く取れない → …~~（不要になった）

### 2-2. Nice-to-have（あれば運用が楽）

| # | 項目 | 裏どり結果 | 当店側の格納先 | KDS側の受け皿 |
| --- | --- | --- | --- | --- |
| 7 | メニューのオプション・備考 | `ReservationOrder` に専用フィールド無し。オプション表現はメニュー名文字列に含まれる可能性（要目視確認） | `reservation_items.options_text` | `stock[].menu[].options` ※受け口は実装済み |
| 8 | アレルギー情報 | ⚠️ **一部確定（2026-07-16）**：専用フィールド無し。`special_request`（自由記述、公式説明に "dietary restrictions, etc." と明記）に混在。ただし `questions`（カスタムアンケート `id`/`question`/`answer` の配列）に店舗側で「アレルギー」設問を設定していれば構造化されて入る可能性あり → **店舗のTableCheck予約フォーム設定を打合せで確認** | `reservation_items.allergies_text` | `stock[].menu[].allergies` ※受け口は実装済み（`allergies`/`allergy`/`allergyInfo` の表記ゆれ対応あり） |
| 9 | メニュー商品コード/SKU | `ReservationOrder` に商品コード相当のフィールド無し（`id`はorder自体のID） | `reservation_items.menu_item_id`（`menu_items.item_code` と突合） | レーン分類（rice/side/drink）の精度向上 |
| 10 | 希望席タイプ・席枠 | ✅ **確定（2026-07-16）**：`seat_types`（"table, counter, private room, etc." の配列/カンマ区切り） | `reservation_slots` | （KDSでは未使用。座席GUI用） |
| 11 | 連絡先（電話番号） | スキーマ要確認 | 未設計（v2扱い） | — |
| 12 | 予約の作成・更新日時 | SyncEvent に `created_at` あり | 差分検知に利用 | — |

---

## 3. 確定した技術仕様（裏どり済み）

| 項目 | 確定内容 |
| --- | --- |
| 取得方式 | **ポーリング（pull）で成立**。`GET /sync_events?deliver=true` → 返ってきた `syncable_id` で `GET /reservations/{id}`（2段構え。SyncEvent自体に予約データは入っていない） |
| ポーリング間隔 | **30秒に1回より高頻度は不可**（TableCheck指定）。時間範囲指定は最大24時間・最大1000件/クエリ・ページネーション無し |
| イベント処理 | `created` / `updated` は **upsert**（IDが無ければ作成・あれば更新）で統一処理を推奨。`deleted` は該当ID無しなら無視。404が返ったら削除済みとみなす |
| 認証 | 環境ごとに発行される **secret_key**。全リクエストHTTPS必須。JSON形式のREST API |
| レート制限 | コンポーネントごとに **10,000リクエスト/5分**。超過で HTTP 429。当店規模では問題なし |
| テスト環境 | 基本は本番のみだが、**依頼すればTest/Liveの別アカウントを用意してもらえる**（api@tablecheck.com） |
| URL | **https://api.tablecheck.com/api/...** を使う。旧 tablesolution.com 系は2026年廃止（2026-06-03以降は308リダイレクト） |
| 料金 | 公開料金表なし。「料金は変動し、申請プロセス内で個別提示」→ Issue #74 のとおり打合せでしか確定しない |
| アクセス申請 | api@tablecheck.com へメール → APIポータルで利用目的・構成図・月間リクエスト数見積を提出 → 承認後にクレデンシャル発行。**Shopごとにアクセス申請が必要** |

---

## 4. KDS側の受け皿（中継サーバーが出力すべき形式）

### 4-1. 予約ストック `kds_stock_v1` ← TableCheck連携の直接の流し込み先

```js
[{
  rid: "予約ID",                    // TableCheck予約IDをそのまま or マッピング
  time: "18:30",                    // 来店予定（start_at をJST変換）
  adults: 2, kids: 1,               // 内訳（取れなければ合計をadultsに）
  name: "山田",                     // 予約者名
  menu: [{                          // 事前メニュー。空配列の予約はストックに載せない
    name: "山城牛の焼きすき土鍋御膳",
    qty: 2,
    options: "ご飯大盛り" | null,   // ※受け口は実装済み
    allergies: "えび" | null        // ※受け口は実装済み
  }],
  seenAt: 1234567890                // 取込時刻（KDS側で付与）
}]
```

- **status / updated_at はKDSに持たせない**。キャンセル・変更は中継サーバー側で判断し、ストックからの削除・上書きとして反映する。

### 4-2. 注文カード `window.KDS_ORDERS` ← 予約は「着手」操作でこちらに変換される

```js
[{
  id: "res-" + rid,                 // 予約由来は "res-" プレフィックスで紐付け
  table: "5" | null,                // 来店前はnull（「卓: —」表示）
  type: "reserved",                 // 予約由来は "reserved"（カード金色）
  start: Date.now(),                // 着手時刻
  people: 3, adults: 2, kids: 1,    // 「N名 幼M」表示のため内訳も保持
  items: [{ name, qty, options, allergies, done: false }]
}]
```

変換はKDS内の `moveResToMain()` が担うため、**中継サーバーは 4-1 の形だけ作ればよい**。

---

## 5. 打合せ / APIコンソールで潰す残課題（優先順）

1. ~~🔴 `GET /reservations` のレスポンスに**コース/メニュー情報が構造化されて入るか**~~ → **✅ 2026-07-16 解決（§6参照）**。`orders[].menu_item_name_translations`/`qty` で確定
2. ~~⚠️ **大人/子供の内訳**フィールドの有無~~ → **✅ 2026-07-16 解決（§6参照）**。`pax_adult`/`pax_child` で確定
3. ⚠️ アレルギー・要望が**どのフィールドで返るか**（`special_request` 自由記述は確定。`questions` カスタムアンケートの設定有無は店舗設定次第 → 打合せで要確認）
4. 料金（初期/月額/従量・プラン差）— 打合せでしか確定しない
5. 予約通知メールの可否（Messaging v1 の契約要否として確認）— 打合せでしか確定しない

→ 1・2は事前予告どおり、打合せを待たずAPIコンソール（Swagger UI）実機確認で自己解決できた（§6）。残る3〜5は打合せ必須。

---

## 6. 【2026-07-16 追記】APIコンソール実機確認で確定した事項

`https://api.tablecheck.com/api/booking/v1/docs`（Booking v1 の Swagger UI）を実際に開き、`Reservation` / `ReservationOrder` スキーマを展開して裏どりした結果。

### Reservation スキーマの主要フィールド（実機確認）

| フィールド | 型 | 内容 |
| --- | --- | --- |
| `pax` | integer | 合計人数 |
| `pax_adult` | integer | 大人人数（例: 3） |
| `pax_senior` | integer | シニア人数 |
| `pax_child` | integer | 子供人数（例: 0） |
| `pax_baby` | integer | 乳幼児人数 |
| `orders` | `ReservationOrder[]` | 「The ordered items in the reservation.」— 事前注文/メニュー明細 |
| `questions` | array | 「Optional array of custom questions and answers (enquetes). Each item may include id, question, answer.」— 店舗が設定したカスタム設問への回答 |
| `special_request` | string | 「Free-text additional request made by the customer at time of booking. May be used to include special requests, dietary restrictions, etc.」 |
| `status` | string(enum) | `tentative, pending, request, accepted, confirmed, attended, cancelled, noshow, rejected, iou_prepay, iou_auth` |
| `seat_types` | array | 「Indicates what kind of seating the customer would like to receive, e.g. table, counter, private room, etc.」 |
| `first_name` / `last_name` | string | 予約者名（`alt_first_name`/`alt_last_name` はふりがな用途と推測） |
| `phone` / `sms_number` / `email` | string | 連絡先 |

### ReservationOrder スキーマ（`orders` の要素）

```
{
  id,
  menu_item_name_translations,  // 例: {"en": "Sushi", "ja": "寿司"}
  price,
  qty
}
```

**結論**：事前メニューは `orders[]` として品名（多言語）・数量・単価が構造化データで取得できる。memoパース（§4-1で想定していたフォールバック）は不要になる見込み。ただし「オプション（大盛り等）」「アレルギー」は `ReservationOrder` に専用フィールドが無いため、`special_request` または `questions`（店舗が設問を設定していれば）から拾う設計になる。

### 残る不確実性（打合せ/実データで最終確認）

- `questions` に実際に「アレルギー」相当の設問が設定されているか（店舗のTableCheck予約フォーム設定次第。設定されていなければ `special_request` の自由記述パースに頼ることになる）
- `menu_item_name_translations` が実データで本当に空にならないか（コース無しの「席のみ予約」では `orders` が空配列になる想定）

---

## 出典

- TableCheck API Confluence: 概要 / Booking v1 / Sync v1 / Site Controller v1 / Distribution v1 / Getting Started / 各ページ（2026-07-15 閲覧）
- リポジトリ内: `kds-a-grid.html`（実装精査）/ `docs/db-design.md` / `deshup-spec.md` / `knowledge/2026-06-04_大城さん向け現状共有・スコープ再確認.md`（push/pull分析）/ `knowledge/2026-06-18_議事録_システム構成・メニュー.md`（ミニPC構成）
- 関連 Issue: #74
